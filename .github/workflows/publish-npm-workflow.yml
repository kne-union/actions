on:
  workflow_call:
    inputs:
      package_name:
        required: true
        type: string
jobs:
  publish-npm:
    runs-on: ubuntu-latest
    steps:
      - run: echo ${{secrets.NODE_AUTH_TOKEN}}
      - run: echo ${{secrets.KNE_PACKAGE_PUBLISH}}
      - uses: actions/download-artifact@v4
        with:
          name: build-dist
          path: ./
      - name: Publish To Npm
        env:
          NODE_AUTH_TOKEN: ${{secrets.KNE_PACKAGE_PUBLISH}}
        run: npm publish --access=public
      - name: Sync To Cnpm
        run: npx cnpm sync ${{inputs.package_name}}
